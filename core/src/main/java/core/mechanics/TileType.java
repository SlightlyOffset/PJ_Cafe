package core.mechanics;

public enum TileType {
    STRAIGHT,
    L_TURN,
    T_JUNCTION,
    CROSS, /*didn't use*/
    STRAIGHT_ROTATABLE,
    L_TURN_ROTATABLE,
    T_JUNCTION_ROTATABLE,
    DEADEND,
    TELEPORT,
    WRONG_TELEPORT,
    EMPYT
}
